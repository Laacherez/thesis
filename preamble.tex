\usepackage[utf8]{inputenc}
\usepackage[
  a4paper,
  inner=2.8cm,
  outer=2.2cm,
  top=2.5cm,
  bottom=2.7cm,
  includehead
]{geometry}
\setlength{\headheight}{14pt}

\newcommand{\currentpublicationtitle}{}

\newcommand{\publicationsection}[1]{%
  \clearpage
  \refstepcounter{section}%
  \renewcommand{\currentpublicationtitle}{#1}%
  \phantomsection
  \addcontentsline{toc}{section}{\protect\numberline{\thesection}#1}%
  \markright{\thesection\quad #1}%
}

\usepackage[hidelinks]{hyperref}

\newcommand{\includepublicationpdf}[2][]{%
  \includepdf[
    pages=-,
    scale=0.85,
    pagecommand={%
      \thispagestyle{fancy}%
      \markright{\thesection\quad \currentpublicationtitle}%
    },
    #1
  ]{#2}%
}

\usepackage{xcolor}

\usepackage{setspace}
\usepackage{parskip}
\setstretch{1.12}

\usepackage[nottoc]{tocbibind} % Add list of fig to TOC
\usepackage{pdfpages}

\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{} 

\fancyhead[LE,RO]{\thepage}

\fancyhead[RE]{\nouppercase{\leftmark}}
\fancyhead[LO]{\nouppercase{\rightmark}}


\fancypagestyle{plain}{
  \fancyhf{}
  \fancyhead[LE,RO]{\thepage}
  \renewcommand{\headrulewidth}{0.3pt}
  \renewcommand{\footrulewidth}{0pt}
}

\usepackage{tikz}
\usepackage{eso-pic}

\newcommand{\chaptertab}{%
  \AddToShipoutPictureBG{%
    \begin{tikzpicture}[remember picture, overlay]
      \ifnum\value{chapter}>0
        \node[
          fill=black,
          text=white,
          font=\sffamily\bfseries\small,
          minimum width=0.8cm,
          minimum height=1.8cm,
          anchor=east
        ] at ([yshift={-2.5cm-\value{chapter}*1.9cm}]current page.north east) {\thechapter};
      \fi
    \end{tikzpicture}%
  }%
} % Chapter little black thing at the side

\usepackage{longtable} % Table continue across pages
\usepackage{algorithm} % Code snipsnips
\usepackage{algpseudocode}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{hyperref}


\usepackage{listings}
\usepackage{upgreek}


\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{red},
    showstringspaces=false,
    frame=single,
    breaklines=true
}
\renewcommand{\lstlistingname}{Snippet} % Alternate code snipsnips ? I odnt remember

\usepackage{esvect}
\usepackage{bm}
\usepackage[backend=biber,style=numeric, sorting=none]{biblatex}
\addbibresource{bibliography/references.bib}